<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:textViewStyle">@style/TextViewStyle</item>
    </style>

    //bottom dialog 둥글게
    <style name="AppBottomSheetDialogTheme"
        parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle"
        parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/dialog_bottom</item>
    </style>

    //dialog bottom sheet_gender
    <style name="dialog_gender">
        <item name="android:textSize">@dimen/_14dp</item>
        <item name="android:fontFamily">@font/font</item>
        <item name="android:layout_marginBottom">@dimen/_18dp</item>
        <item name="android:layout_marginTop">@dimen/_18dp</item>
    </style>

    //Header
    <style name="Header">
        <item name="android:textSize">26dp</item>
        <item name="android:fontFamily">@font/font</item>
        <item name="android:paddingVertical">30dp</item>
    </style>

    //string
    <style name="main_string">
        <item name="android:fontFamily">@font/font</item>
    </style>
    <style name="Picker">
        <item name="android:textSize">25dp</item></style>

    <style name="TextViewStyle">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">14dp</item>
        <item name="android:fontFamily">@font/font</item>
    </style>

</resources>
